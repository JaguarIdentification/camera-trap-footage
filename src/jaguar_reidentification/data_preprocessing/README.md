# Data Preprocessing

This module contains scripts for preprocessing the raw video data for jaguar re-identification. The preprocessing steps include:
1. Cleaning the .csv metadata/label file (Remove invalid entries, fix typos, standardize formats): `clean_labels.py`
2. Copying and preprocessing the video files (e.g. format conversion): `preprocess_videos.py`

